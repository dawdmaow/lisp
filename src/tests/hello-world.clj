;; Functions are called by placing them as the first elemenet a paranthesis pair.
;; The other elements are the arguments to the function.
;; `echo` is a function that prints its arguments to the console.
(echo "Hello world!")